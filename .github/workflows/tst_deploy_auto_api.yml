name: TST - Despliegue automático de API planificado

on:
  schedule:
    - cron: '0 6 * * *'   # 07:00 hora España (06:00 UTC)
    - cron: '0 18 * * *'  # 19:00 hora España (18:00 UTC)
  workflow_dispatch:        # Permite ejecución manual desde GitHub Actions
  push:
    branches:
      - tst                 # Solo cuando hay commits en la rama tst

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clonar el repositorio
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      # 2️⃣ Configurar Node.js
      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3️⃣ Empaquetar solo lo necesario (sin archivos sensibles)
      - name: 📁 Prepare deployment package
        run: |
          mkdir deploy
          rsync -av --delete \
                    --exclude='.git' \
                    --exclude='.github' \
                    --exclude='node_modules' \
                    --exclude='.env' \
                    --exclude='.env.key' \
                    --exclude='*.log' \
                    ./ ./deploy/

      # 4️⃣ Subir archivos a AlwaysData vía SSH (autenticación por clave)
      - name: 🚀 Deploy to AlwaysData via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST_TST }}
          username: ${{ secrets.SSH_USER_TST }}
          key: ${{ secrets.SSH_KEY_TST }}
          source: "deploy/*"
          target: "/home/tstsite/www/api"
          overwrite: true
          strip_components: 1

      # 5️⃣ Limpieza total, reinstalación y reinicio del servicio remoto
      - name: 🔁 Clean, Install & Restart AlwaysData service
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST_TST }}
          username: ${{ secrets.SSH_USER_TST }}
          key: ${{ secrets.SSH_KEY_TST }}
          script: |
            cd /home/tstsite/www/api

            echo "🧹 Limpiando instalación anterior..."
            rm -rf node_modules package-lock.json
            npm cache clean --force

            echo "📦 Instalando dependencias de producción..."
            npm install --omit=dev --no-audit --no-fund --silent

            echo "⚡ Optimizando permisos y entorno..."
            chmod -R 755 .
            export NODE_ENV=production

            echo "♻️ Reiniciando servicio..."
            pkill -f "node.*bin/www" || true
            nohup node bin/www > app.log 2>&1 &

            echo "✅ API desplegada, cache limpia y servicio reiniciado correctamente."
